# Copyright 2026 Ari Sulistiono
# SPDX-License-Identifier: Apache-2.0
name: Build and protocol smoke tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore ARIEC60870.sln

      - name: Build
        run: dotnet build ARIEC60870.sln --configuration Release --no-restore

      - name: Run dependency-free protocol smoke tests
        run: dotnet run --project tests/ARIEC60870.Protocol.Tests/ARIEC60870.Protocol.Tests.csproj --configuration Release --no-build
