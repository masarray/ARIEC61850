<Window x:Class="AR.Iec61850.IedDiscovery.DiscoverIedWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Discover IED" Height="560" Width="760" MinHeight="520" MinWidth="720"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize" Background="#F4F7FB" FontFamily="Segoe UI">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#253247" />
            <Setter Property="FontSize" Value="12" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="MinHeight" Value="27" />
            <Setter Property="Padding" Value="7,3" />
            <Setter Property="BorderBrush" Value="#AEB9C7" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="MinWidth" Value="96" />
            <Setter Property="Margin" Value="8,0,0,0" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="56" />
        </Grid.RowDefinitions>
        <Grid Margin="14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" />
                <ColumnDefinition Width="14" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Background="White" BorderBrush="#D5DDE8" BorderThickness="1" Padding="12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Text="Connection parameters" FontSize="15" FontWeight="SemiBold" Foreground="#0F172A" />
                    <Grid Grid.Row="1" Margin="0,14,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="92" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="IP address" VerticalAlignment="Center" />
                        <TextBox Grid.Column="1" Text="{Binding Host, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Grid.Row="1" Text="Port" Margin="0,8,0,0" VerticalAlignment="Center" />
                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Port}" Margin="0,8,0,0" />
                        <TextBlock Grid.Row="2" Text="IED name" Margin="0,8,0,0" VerticalAlignment="Center" />
                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Name}" IsReadOnly="True" ToolTip="Discovered from the IED model after a successful connection." Margin="0,8,0,0" />
                        <TextBlock Grid.Row="3" Text="Timeout" Margin="0,8,0,0" VerticalAlignment="Center" />
                        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding TimeoutMs}" Margin="0,8,0,0" />
                    </Grid>

                    <Expander Grid.Row="2" Header="Advanced parameters" IsExpanded="False" Margin="0,18,0,0">
                        <Grid Margin="0,8,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="92" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="AP Title" />
                            <TextBox Grid.Column="1" Text="{Binding ApTitle}" />
                            <TextBlock Grid.Row="1" Text="AE Qualifier" Margin="0,7,0,0" />
                            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding AeQualifier}" Margin="0,7,0,0" />
                            <TextBlock Grid.Row="2" Text="P Selector" Margin="0,7,0,0" />
                            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding PSelector}" Margin="0,7,0,0" />
                            <TextBlock Grid.Row="3" Text="S Selector" Margin="0,7,0,0" />
                            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding SSelector}" Margin="0,7,0,0" />
                            <TextBlock Grid.Row="4" Text="T Selector" Margin="0,7,0,0" />
                            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding TSelector}" Margin="0,7,0,0" />
                            <CheckBox Grid.Row="5" Grid.ColumnSpan="2" Content="Read DataSet directories" IsChecked="{Binding ReadDataSetDirectories}" Margin="0,12,0,0" />
                            <CheckBox Grid.Row="6" Grid.ColumnSpan="2" Content="Probe RCB attributes" IsChecked="{Binding ProbeReportAttributes}" Margin="0,6,0,0" />
                            <CheckBox Grid.Row="7" Grid.ColumnSpan="2" Content="Read file directory" IsChecked="{Binding ReadFileDirectory}" Margin="0,6,0,0" />
                        </Grid>
                    </Expander>
                </Grid>
            </Border>

            <Border Grid.Column="2" Background="White" BorderBrush="#D5DDE8" BorderThickness="1" Padding="12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Text="Previous connections" FontSize="15" FontWeight="SemiBold" Foreground="#0F172A" />
                    <DataGrid Grid.Row="1" ItemsSource="{Binding PreviousConnections}" SelectedItem="{Binding SelectedPreviousConnection}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="None" RowHeight="28" BorderThickness="0" Margin="0,12,0,0" MouseDoubleClick="PreviousConnections_MouseDoubleClick">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="IP address" Binding="{Binding Host}" Width="*" />
                            <DataGridTextColumn Header="Port" Binding="{Binding Port}" Width="70" />
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="1" Background="#E8EDF5" BorderBrush="#D5DDE8" BorderThickness="0,1,0,0" Padding="12">
            <DockPanel LastChildFill="False">
                <TextBlock DockPanel.Dock="Left" Text="Discovery runs asynchronously and can be cancelled from the main shell." VerticalAlignment="Center" Foreground="#55657B" />
                <Button DockPanel.Dock="Right" Content="Cancel" Click="Cancel_Click" />
                <Button DockPanel.Dock="Right" Content="Discover" Click="Discover_Click" Background="#0B62A4" Foreground="White" BorderBrush="#0B62A4" />
                <Button DockPanel.Dock="Right" Content="Save" Click="Save_Click" />
            </DockPanel>
        </Border>
    </Grid>
</Window>
